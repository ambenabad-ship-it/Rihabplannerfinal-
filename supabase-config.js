// Supabase configuration. Fill in the two values below from your Supabase
// project (Settings → API). Use the ANON public key — never the service-role.
//
// 1) Open https://supabase.com → your project → Settings → API
// 2) Copy "Project URL" into SUPABASE_URL
// 3) Copy "anon / public" key into SUPABASE_ANON_KEY
// 4) Save and reload the app
//
// Until you fill these in, the app falls back to localStorage (single device).
// Once filled in, signed-in users see their planning history on every device.

window.SUPABASE_URL      = 'https://oowuhdyogsfrjjaqfong.supabase.co';
window.SUPABASE_ANON_KEY = 'sb_publishable_HscdnF14BqbWdEndAfoDRQ_e_JyHYAh';
